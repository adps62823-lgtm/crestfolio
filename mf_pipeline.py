"""Legacy mutual fund pipeline entry point.

The active implementation now lives in `src/server/seed.ts` and the Next.js app.
"""

if __name__ == "__main__":
    print("Legacy pipeline removed. Crestfolio now runs through the web app.")
